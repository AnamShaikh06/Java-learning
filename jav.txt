jdk : java developemnt kit:  tool kit requires to run the java program on system.

jdk= jre+jvm

jvm + libraries = jre
jre + development tools = jdk

two ways java code runs:
1.compilation : source code(.java) then jdk converts it in byte code(.class)
2. execution : byte code(.class) then jvm converts to native code(machine knows: eg:0,1)


**Byte code we can run in any system provided that it contains jre


**Datatypes:
1. Primitive: byte,short,char,boolean,int,long,float,double
2. Non-Primitive: String,Array,Class,Object,Interface


next(): takes 1 token means only one word
nextLine(): takes the whole sentence as input
nextInt(): take integer


**if else:
if (cond){}
else if(){}
else{}


**switch case:

switch(variable){
    case 1:
    ///
    break;
    case 2:
    break;
    default:
    
}

for loop:

for(ini;condi;incre/decre){

}

while:

while(condi){

}

dowhile:
do{}
while(condi)


* in java is only for numeric multiplication but not for 
"*" * 4  is wrong
"*" .repeat(4)


**function:

returntype functioname(type arg1,type arg){

}
function stored in a memory are in the form of stack
camelcase: myNameIsAnam

***functions are called directly whereas methods are called with the help
of class objects.

**new keyword: it is used to allocate a new space inside the memory

**Array:
type[] arrayName =new type[size];
eg: if size of array is 3 and it is of intger array
then in memory it takes 4*3=12bytes of space in total.

if we already know the size of array then we can define it like this

type [] arrayname={1,2,3,5};

**Null:
by default if we create a variable then it stores null in java
like for obj it stores null, int stores 0, float stores 0.0, boolean false, string ""

**size of array:  arrayname.length

**max and min:
Integer.MAX_VALUE: stores max int value ie 2^31 -1
Integer.MIN_VALUE: stores min value ie -2^31

**2D array:
memory space of 2D array
if we have 3*5 array of integer type: then space will be of  3*5*4=60bytes 

type[][] arrayname=new type[rows][col]


**String:
stringname.length()= print the length of string
stringmame.chartAt(index)= give the character present at that index

string1.compare(string2): to compare the string 
if string1<string2: print +ve value
if string1>string2: print -ve value
if equal the prints 0

it compare character by character in both the string and if found greater one than that string wins


**substring(begin index,end index): like slicing in python
